import React from 'react';
import { Nav, Navbar, NavItem } from 'react-bootstrap';

const Footer = () => (
  <Navbar>
    <Nav>
      <NavItem>
        DoSomething.org
      </NavItem>
    </Nav>
  </Navbar>
);

export default Footer;
